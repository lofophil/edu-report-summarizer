# Core ML / fine-tuning
torch>=2.0
transformers>=4.40
peft>=0.10
accelerate>=0.27

# Retrieval / embeddings
sentence-transformers>=2.6
faiss-cpu          # optional — for large-scale ANN search

# Document processing
python-docx>=1.1
pdf2docx>=0.5.7
comtypes           # Windows only — Word COM automation

# Utilities
numpy>=1.24
tqdm>=4.66
